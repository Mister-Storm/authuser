server:
  port: 8087
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ead_authuser
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true